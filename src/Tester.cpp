#include "Tester.h"

Tester::Tester(int id, string name, string occupation, int hourly_payment , int project_contribution, Project* current_project)  :
        Engineer(id, name, occupation, hourly_payment , project_contribution, current_project)
{
    /*
    this->id = id;
    this->name = name;
    this->occupation = occupation;
    this->hourly_payment = hourly_payment;
    this->project_contribution = project_contribution;
    this->current_project = current_project;
     */
}